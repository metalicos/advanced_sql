SELECT DISTINCT R.STUDENT_ID
FROM EXAM_RESULT R WHERE
	(SELECT COUNT(S.SUBJECT_ID)
		FROM EXAM_RESULT S
		WHERE S.STUDENT_ID = R.STUDENT_ID
			AND R.MARK > 3
			AND S.SUBJECT_ID <> R.SUBJECT_ID) >= 2 